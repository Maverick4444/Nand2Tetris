/** Initializes a new SquareCircle game and starts running it. */
class Main {
    function void main() {
        var SquareCircleGame game;
        let game = SquareCircleGame.new();
	do game.SquareRun();
        do game.dispose();
        return;
    }
}
